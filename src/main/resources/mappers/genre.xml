<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="genre">
	<select id="list" resultType="Genre">
		SELECT genre.code, genre.contents, SUM(novel.recom) AS recom
		FROM genre
		JOIN novel ON genre.code=novel.genre
		GROUP BY genre.code, genre.contents
		ORDER BY SUM(novel.recom) DESC
		LIMIT #{offset}, #{perPage}
	</select>
	<select id="total" resultType="Integer">
		SELECT COUNT(*)
		FROM genre
	</select>
</mapper>