<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="genre">
	<select id="list" resultType="Genre">
		SELECT genre.code, genre.contents, SUM((SELECT COUNT(*) AS recom FROM recommend WHERE novel.code=recommend.novel)) AS recom
		FROM genre
		JOIN novel ON genre.code=novel.genre
		GROUP BY genre.code, genre.contents
		ORDER BY recom DESC
		LIMIT #{offset}, #{perPage}
	</select>
	<select id="total" resultType="Integer">
		SELECT COUNT(*)
		FROM genre
	</select>
	<insert id="add">
		INSERT INTO genre
		VALUES(NULL, #{contents})
		<selectKey keyProperty="code" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	<select id="item" resultType="Genre">
		SELECT genre.code, genre.contents, SUM((SELECT COUNT(*) AS recom FROM recommend WHERE novel.code=recommend.novel)) AS recom
		FROM genre
		JOIN novel ON genre.code=novel.genre
		WHERE genre.code=#{code}
		GROUP BY genre.code, genre.contents
	</select>
</mapper>